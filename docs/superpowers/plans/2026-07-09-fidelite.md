# Carte de fidélité — Plan (Bloc 5)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`).

**Goal:** Carte à tampons : 1 tampon/commande payée (identité = téléphone), récompense après N tampons, progression côté client + gestion dashboard. Pro/Max.

**Architecture:** App `base`. `LoyaltyProgram` (OneToOne resto) + `LoyaltyCard` + `Order.loyalty_awarded`. Service `base/services/loyalty.py`. Accrual au paiement (cashier). Config en Paramètres. Affichage client sur la page de succès. Onglet dashboard.

**Tech Stack:** Django, templates Tailwind.

## Global Constraints

- venv shebang cassé → `./env/bin/python3`.
- Tests : `./env/bin/python3 manage.py test base.test_fidelite -v 2` ; suite `./env/bin/python3 manage.py test base --noinput`.
- Gating : `restaurant.is_pro()` + owner/coadmin pour config/dashboard/affichage. Accrual gaté par `is_enabled` du programme (+ Pro via `program_for`), pas par rôle.
- Identité = `Order.customer_phone` (E.164). Idempotence accrual via `Order.loyalty_awarded`.
- Helpers test : `make_user`, `make_restaurant` (base.tests) ; helper `make_pro` local dans le nouveau fichier de test.
- Réutilise patterns : `@owner_or_coadmin_required`, `Http404`, section Paramètres « Communauté & avis », success page `confirmation.html` (bloc 1).

---

### Task 1: Modèles LoyaltyProgram / LoyaltyCard / Order.loyalty_awarded

**Files:** Modify `base/models.py` ; migration ; Test `base/test_fidelite.py`.

**Interfaces:** Produces `LoyaltyProgram` (OneToOne `restaurant`, `related_name='loyalty'`), `LoyaltyCard` (unique restaurant+phone), `Order.loyalty_awarded`.

- [ ] **Step 1: Test (échec attendu)**

Créer `base/test_fidelite.py` :

```python
from django.test import TestCase
from base.models import LoyaltyProgram, LoyaltyCard, Order
from base.tests import make_user, make_restaurant


class LoyaltyModelsTest(TestCase):
    def test_program_defaults(self):
        resto = make_restaurant(make_user())
        prog = LoyaltyProgram.objects.create(restaurant=resto)
        self.assertFalse(prog.is_enabled)
        self.assertEqual(prog.stamps_required, 10)
        self.assertEqual(resto.loyalty, prog)

    def test_card_unique_per_phone(self):
        resto = make_restaurant(make_user())
        LoyaltyCard.objects.create(restaurant=resto, phone="+2290100000000", stamps=3)
        self.assertEqual(resto.loyalty_cards.count(), 1)

    def test_order_loyalty_awarded_default_false(self):
        resto = make_restaurant(make_user())
        o = Order.objects.create(restaurant=resto, customer_phone="+2290100000000")
        self.assertFalse(o.loyalty_awarded)
```

- [ ] **Step 2: Lancer (échec)** — `./env/bin/python3 manage.py test base.test_fidelite -v 2`

- [ ] **Step 3: Modèles**

Dans `base/models.py`, ajouter à `Order` (près des autres champs booléens, ex.
après `is_paid`/`paid_by_name`) :

```python
    loyalty_awarded = models.BooleanField(default=False)
```

Puis, à la fin du fichier :

```python
class LoyaltyProgram(models.Model):
    """Programme de fidélité (carte à tampons) d'un restaurant."""
    restaurant = models.OneToOneField(
        Restaurant, on_delete=models.CASCADE, related_name='loyalty')
    is_enabled = models.BooleanField(default=False)
    stamps_required = models.PositiveIntegerField(default=10)
    reward_label = models.CharField(max_length=100, default='1 récompense')
    updated_at = models.DateTimeField(auto_now=True)

    def __str__(self):
        return f"Fidélité {self.restaurant.name}"


class LoyaltyCard(models.Model):
    """Carte à tampons d'un client (identifié par téléphone) chez un resto."""
    restaurant = models.ForeignKey(
        Restaurant, on_delete=models.CASCADE, related_name='loyalty_cards')
    phone = models.CharField(max_length=20)
    stamps = models.PositiveIntegerField(default=0)
    rewards_redeemed = models.PositiveIntegerField(default=0)
    updated_at = models.DateTimeField(auto_now=True)

    class Meta:
        unique_together = ('restaurant', 'phone')
        ordering = ['-stamps']

    def __str__(self):
        return f"Carte {self.phone} ({self.stamps} tampons)"
```

- [ ] **Step 4: Migration + migrate**

```bash
./env/bin/python3 manage.py makemigrations base
./env/bin/python3 manage.py migrate
```

- [ ] **Step 5: Lancer (succès)** — 3 tests PASS.

- [ ] **Step 6: Commit**

```bash
git add base/models.py base/migrations/ base/test_fidelite.py
git commit -m "feat: modèles fidélité (LoyaltyProgram, LoyaltyCard, Order.loyalty_awarded)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: Service loyalty (award / redeem / progress)

**Files:** Create `base/services/loyalty.py` ; Test `base/test_fidelite.py`.

**Interfaces:** Produces `loyalty.program_for`, `loyalty.award_for_order`, `loyalty.redeem`, `loyalty.progress`.

- [ ] **Step 1: Tests (échec attendu)**

Ajouter à `base/test_fidelite.py` :

```python
from base.services import loyalty
from base.models import Category, MenuItem


def _prog(resto, enabled=True, required=3):
    return LoyaltyProgram.objects.create(
        restaurant=resto, is_enabled=enabled, stamps_required=required,
        reward_label="1 plat offert")


def _order(resto, phone="+2290100000000"):
    return Order.objects.create(restaurant=resto, customer_phone=phone)


class LoyaltyServiceTest(TestCase):
    def setUp(self):
        self.resto = make_restaurant(make_user())

    def test_award_credits_one_stamp_idempotent(self):
        _prog(self.resto)
        o = _order(self.resto)
        card = loyalty.award_for_order(o)
        self.assertEqual(card.stamps, 1)
        o.refresh_from_db()
        self.assertTrue(o.loyalty_awarded)
        # 2e appel : pas de recrédit
        loyalty.award_for_order(o)
        card.refresh_from_db()
        self.assertEqual(card.stamps, 1)

    def test_award_noop_if_disabled_or_no_phone(self):
        _prog(self.resto, enabled=False)
        self.assertIsNone(loyalty.award_for_order(_order(self.resto)))
        _prog(self.resto).is_enabled  # noop
        LoyaltyProgram.objects.filter(restaurant=self.resto).update(is_enabled=True)
        self.assertIsNone(loyalty.award_for_order(_order(self.resto, phone="")))

    def test_redeem_only_when_threshold_reached(self):
        _prog(self.resto, required=3)
        card = LoyaltyCard.objects.create(restaurant=self.resto, phone="+2290100000000", stamps=3)
        self.assertTrue(loyalty.redeem(card))
        card.refresh_from_db()
        self.assertEqual(card.stamps, 0)
        self.assertEqual(card.rewards_redeemed, 1)
        self.assertFalse(loyalty.redeem(card))  # plus assez

    def test_progress(self):
        _prog(self.resto, required=3)
        LoyaltyCard.objects.create(restaurant=self.resto, phone="+2290100000000", stamps=3)
        p = loyalty.progress(self.resto, "+2290100000000")
        self.assertEqual(p["stamps"], 3)
        self.assertEqual(p["required"], 3)
        self.assertEqual(p["remaining"], 0)
        self.assertTrue(p["reward_available"])
        self.assertIsNone(loyalty.progress(make_restaurant(make_user()), "+2290100000000"))
```

- [ ] **Step 2: Lancer (échec)** — module inexistant.

- [ ] **Step 3: Service**

Créer `base/services/loyalty.py` :

```python
"""Logique carte de fidélité (tampons)."""
from base.models import LoyaltyProgram, LoyaltyCard


def program_for(restaurant):
    prog = LoyaltyProgram.objects.filter(restaurant=restaurant).first()
    if prog is None or not prog.is_enabled:
        return None
    return prog


def award_for_order(order):
    """Crédite 1 tampon pour une commande payée. Idempotent."""
    if order.loyalty_awarded or not order.customer_phone:
        return None
    prog = program_for(order.restaurant)
    if prog is None:
        return None
    card, _ = LoyaltyCard.objects.get_or_create(
        restaurant=order.restaurant, phone=order.customer_phone)
    card.stamps += 1
    card.save(update_fields=["stamps", "updated_at"])
    order.loyalty_awarded = True
    order.save(update_fields=["loyalty_awarded"])
    return card


def redeem(card):
    prog = LoyaltyProgram.objects.filter(restaurant=card.restaurant).first()
    required = prog.stamps_required if prog else 0
    if not required or card.stamps < required:
        return False
    card.stamps -= required
    card.rewards_redeemed += 1
    card.save(update_fields=["stamps", "rewards_redeemed", "updated_at"])
    return True


def progress(restaurant, phone):
    prog = program_for(restaurant)
    if prog is None or not phone:
        return None
    card = LoyaltyCard.objects.filter(restaurant=restaurant, phone=phone).first()
    stamps = card.stamps if card else 0
    return {
        "stamps": stamps,
        "required": prog.stamps_required,
        "remaining": max(0, prog.stamps_required - stamps),
        "reward_available": stamps >= prog.stamps_required,
        "reward_label": prog.reward_label,
    }
```

- [ ] **Step 4: Lancer (succès)** — 4 tests PASS.

- [ ] **Step 5: Commit**

```bash
git add base/services/loyalty.py base/test_fidelite.py
git commit -m "feat: service fidélité (award idempotent, redeem, progress)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: Config Paramètres + accrual (cashier) + affichage client

**Files:** Modify `base/views.py` (`restaurant_settings`, `order_confirmation` est dans `customer/views.py`), `base/cashier_views.py`, `templates/admin_user/settings.html`, `customer/views.py`, `templates/customer/confirmation.html` ; Test `base/test_fidelite.py`.

**Interfaces:** Consumes `loyalty.award_for_order`, `loyalty.progress`, `LoyaltyProgram`.

- [ ] **Step 1: Tests (échec attendu)**

Ajouter à `base/test_fidelite.py` :

```python
from base.cashier_views import mark_order_paid  # noqa (import sanity)


class AccrualTest(TestCase):
    def setUp(self):
        self.resto = make_restaurant(make_user())
        _prog(self.resto, required=10)

    def test_paying_order_awards_stamp(self):
        from base.models import LoyaltyCard
        o = Order.objects.create(
            restaurant=self.resto, customer_phone="+2290100000000",
            status="ready", total=1000)
        loyalty.award_for_order(o)  # simule l'accrual déclenché au paiement
        self.assertEqual(
            LoyaltyCard.objects.get(restaurant=self.resto, phone="+2290100000000").stamps, 1)
```

(Le test unitaire de l'accrual via la vue cashier dépend du harnais multi-tenant ;
on teste ici la logique `award_for_order`. La vue est câblée aux Steps 3-4.)

- [ ] **Step 2: Lancer (échec attendu si logique absente ; ici le service existe déjà donc ce test PASSE — sert de garde de non-régression).**

Run: `./env/bin/python3 manage.py test base.test_fidelite.AccrualTest -v 2`

- [ ] **Step 3: Accrual dans le cashier**

Dans `base/cashier_views.py`, en haut : `from base.services import loyalty`.

Dans `mark_order_paid`, juste après le `order.save(update_fields=[...])` (dans le
`if not order.is_paid ...`), ajouter :

```python
        loyalty.award_for_order(order)
```

Dans `mark_table_paid`, dans la boucle, après le `o.save(update_fields=[...])` de
chaque commande encaissée, ajouter :

```python
        loyalty.award_for_order(o)
```

- [ ] **Step 4: Config dans restaurant_settings**

Dans `base/views.py`, vue `restaurant_settings`, dans le bloc POST après les
champs `whatsapp_community_url`/`google_place_id`, ajouter :

```python
        from base.models import LoyaltyProgram
        prog, _ = LoyaltyProgram.objects.get_or_create(restaurant=restaurant)
        prog.is_enabled = request.POST.get("loyalty_enabled") == "on"
        try:
            prog.stamps_required = max(1, int(request.POST.get("stamps_required", 10)))
        except (TypeError, ValueError):
            prog.stamps_required = 10
        prog.reward_label = request.POST.get("reward_label", "").strip() or "1 récompense"
        prog.save()
```

Et exposer le programme au template : dans le `context` du `render` de
`restaurant_settings`, ajouter :

```python
        "loyalty": LoyaltyProgram.objects.filter(restaurant=restaurant).first(),
```

(importer/charger `LoyaltyProgram` en haut de la vue si besoin.)

- [ ] **Step 5: Section Fidélité dans settings.html**

Dans `templates/admin_user/settings.html`, après la section « Communauté &
avis », ajouter (gatée Pro) :

```html
<div class="mt-8 border-t border-slate-200 pt-6">
  <h3 class="text-sm font-semibold text-slate-700 mb-1">Carte de fidélité</h3>
  <p class="text-xs text-slate-500 mb-4">
    {% if restaurant.is_pro %}1 tampon par commande payée. Récompense au seuil atteint.
    {% else %}Disponible avec l'offre <strong>Pro</strong>.{% endif %}
  </p>
  <div class="space-y-4 {% if not restaurant.is_pro %}opacity-50 pointer-events-none{% endif %}">
    <label class="flex items-center gap-2 text-sm">
      <input type="checkbox" name="loyalty_enabled" {% if loyalty.is_enabled %}checked{% endif %}>
      Activer la carte de fidélité
    </label>
    <div>
      <label class="block text-xs font-semibold uppercase tracking-wide text-slate-500 mb-1">Tampons requis</label>
      <input type="number" name="stamps_required" min="1" value="{{ loyalty.stamps_required|default:10 }}"
             class="w-32 rounded-xl border border-slate-200 px-3 py-2 text-sm" />
    </div>
    <div>
      <label class="block text-xs font-semibold uppercase tracking-wide text-slate-500 mb-1">Récompense</label>
      <input type="text" name="reward_label" maxlength="100" value="{{ loyalty.reward_label|default:'1 récompense' }}"
             placeholder="ex : 1 plat offert"
             class="w-full rounded-xl border border-slate-200 px-3 py-2 text-sm" />
    </div>
  </div>
</div>
```

- [ ] **Step 6: Affichage client (succès)**

Dans `customer/views.py`, vue `order_confirmation`, ajouter au contexte
(après `is_pro`) :

```python
        "loyalty": (
            __import__("base.services.loyalty", fromlist=["progress"]).progress(
                resto, order.customer_phone) if is_pro else None),
```

(Ou proprement : `from base.services import loyalty` en haut du fichier puis
`"loyalty": loyalty.progress(resto, order.customer_phone) if is_pro else None,`.)

Dans `templates/customer/confirmation.html`, dans la zone `{% if is_pro %}`,
ajouter une carte :

```html
{% if loyalty %}
<div class="rounded-2xl bg-indigo-50 border border-indigo-100 p-4 text-center">
  {% if loyalty.reward_available %}
    <p class="font-semibold text-indigo-800">🎁 Récompense disponible !</p>
    <p class="text-sm text-indigo-700 mt-1">{{ loyalty.reward_label }} — montrez ce message au resto.</p>
  {% else %}
    <p class="font-semibold text-indigo-800">Carte de fidélité</p>
    <p class="text-sm text-indigo-700 mt-1">{{ loyalty.stamps }}/{{ loyalty.required }} tampons — encore {{ loyalty.remaining }} pour : {{ loyalty.reward_label }}</p>
  {% endif %}
</div>
{% endif %}
```

- [ ] **Step 7: Lancer les tests** — `./env/bin/python3 manage.py test base.test_fidelite -v 2` PASS.

- [ ] **Step 8: Commit**

```bash
git add base/views.py base/cashier_views.py customer/views.py templates/admin_user/settings.html templates/customer/confirmation.html base/test_fidelite.py
git commit -m "feat: fidélité — config Paramètres, accrual au paiement, progression client

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: Dashboard fidélité (liste + redeem) + sidebar

**Files:** Modify `base/views.py`, `base/urls.py`, `templates/admin_user/sidebar.html` ; Create `templates/admin_user/loyalty/index.html` ; Test `base/test_fidelite.py`.

**Interfaces:** Produces URLs `loyalty_dashboard` (`fidelite/`), `loyalty_redeem` (`fidelite/<int:card_id>/redeem/`).

- [ ] **Step 1: Test (échec attendu)**

Ajouter à `base/test_fidelite.py` :

```python
from django.urls import reverse
from base.models import SubscriptionPlan
from django.utils import timezone
from datetime import timedelta


def make_pro(resto):
    plan = SubscriptionPlan.objects.create(name="Pro", plan_type="pro", price=1)
    resto.subscription_plan = plan
    resto.subscription_end = timezone.now() + timedelta(days=10)
    resto.save()


class LoyaltyDashboardTest(TestCase):
    def setUp(self):
        self.owner = make_user()
        self.resto = make_restaurant(self.owner)
        self.client.force_login(self.owner)
        _prog(self.resto, required=3)

    def _host(self):
        return {"HTTP_HOST": f"{self.resto.subdomain}.localhost"}

    def test_non_pro_forbidden(self):
        resp = self.client.get(reverse("loyalty_dashboard"), **self._host())
        self.assertIn(resp.status_code, (302, 404))

    def test_pro_lists_and_redeems(self):
        make_pro(self.resto)
        card = LoyaltyCard.objects.create(
            restaurant=self.resto, phone="+2290100000000", stamps=3)
        resp = self.client.get(reverse("loyalty_dashboard"), **self._host())
        self.assertEqual(resp.status_code, 200)
        resp2 = self.client.post(
            reverse("loyalty_redeem", args=[card.id]), **self._host())
        self.assertEqual(resp2.status_code, 302)
        card.refresh_from_db()
        self.assertEqual(card.stamps, 0)
        self.assertEqual(card.rewards_redeemed, 1)
```

Note multi-tenant : aligner `_host()` sur le pattern qui passe
(`base/test_imagegen.py`). Skip documenté en dernier recours.

- [ ] **Step 2: Lancer (échec)** — URLs inexistantes.

- [ ] **Step 3: Vues**

Dans `base/views.py`, près de `feedback_list` :

```python
@owner_or_coadmin_required
def loyalty_dashboard(request):
    restaurant = request.restaurant
    if not restaurant.is_pro():
        raise Http404()
    from base.models import LoyaltyProgram
    prog = LoyaltyProgram.objects.filter(restaurant=restaurant).first()
    required = prog.stamps_required if prog else 0
    cards = list(restaurant.loyalty_cards.all())
    for c in cards:
        c.reward_available = required and c.stamps >= required
    return render(request, "admin_user/loyalty/index.html", {
        "restaurant": restaurant, "program": prog, "cards": cards,
    })


@owner_or_coadmin_required
@require_POST
def loyalty_redeem(request, card_id):
    restaurant = request.restaurant
    if not restaurant.is_pro():
        raise Http404()
    from base.models import LoyaltyCard
    from base.services import loyalty
    card = get_object_or_404(LoyaltyCard, id=card_id, restaurant=restaurant)
    if loyalty.redeem(card):
        messages.success(request, f"Récompense enregistrée pour {card.phone}.")
    else:
        messages.error(request, "Pas assez de tampons.")
    return redirect("loyalty_dashboard")
```

- [ ] **Step 4: URLs**

Dans `base/urls.py`, après `retours/` (et importer les vues) :

```python
    path("fidelite/", loyalty_dashboard, name="loyalty_dashboard"),
    path("fidelite/<int:card_id>/redeem/", loyalty_redeem, name="loyalty_redeem"),
```

- [ ] **Step 5: Template**

Créer `templates/admin_user/loyalty/index.html` (blocs `title`, `page_title`,
`content` ; ossature comme `admin_user/feedback/list.html`) :

```html
{% extends "admin_user/base.html" %}
{% block title %}Fidélité — {{ restaurant.name }}{% endblock %}
{% block page_title %}Fidélité{% endblock %}
{% block content %}
<div class="p-4 space-y-4">
  {% if not program or not program.is_enabled %}
    <div class="rounded-xl bg-slate-50 border border-slate-200 p-4 text-sm text-slate-600">
      Activez la carte de fidélité dans <a href="{% url 'restaurant_settings' %}" class="text-primary underline">Paramètres</a>.
    </div>
  {% endif %}
  {% if not cards %}
    <p class="text-sm text-slate-500">Aucune carte pour l'instant.</p>
  {% else %}
    <div class="space-y-2">
      {% for c in cards %}
      <div class="rounded-xl border border-slate-200 bg-white p-3 flex items-center justify-between">
        <div>
          <p class="font-semibold text-slate-800">{{ c.phone }}</p>
          <p class="text-xs text-slate-500">{{ c.stamps }}{% if program %}/{{ program.stamps_required }}{% endif %} tampons · {{ c.rewards_redeemed }} récompense(s) donnée(s)</p>
        </div>
        {% if c.reward_available %}
        <form method="post" action="{% url 'loyalty_redeem' c.id %}">
          {% csrf_token %}
          <button type="submit" class="text-xs rounded-lg bg-indigo-600 text-white px-3 py-1.5 font-semibold">🎁 Récompense donnée</button>
        </form>
        {% endif %}
      </div>
      {% endfor %}
    </div>
  {% endif %}
</div>
{% endblock %}
```

- [ ] **Step 6: Sidebar**

Dans `templates/admin_user/sidebar.html`, déclarer en haut
`{% url 'loyalty_dashboard' as url_loyalty %}` puis, zone owner/coadmin gatée
Pro :

```html
{% if restaurant.is_pro %}
<a href="{{ url_loyalty }}"
   class="flex items-center gap-3 rounded-xl px-3 py-2 text-sm
          {% if current == url_loyalty %}bg-primary text-white shadow-glow{% else %}text-slate-400 hover:bg-white/8 hover:text-white{% endif %}">
  <span>🎟️</span>{% trans "Fidélité" %}
</a>
{% endif %}
```

- [ ] **Step 7: Tests + non-régression**

Run: `./env/bin/python3 manage.py test base.test_fidelite -v 2` → PASS
Puis: `./env/bin/python3 manage.py test base --noinput` → aucune régression.

- [ ] **Step 8: Commit**

```bash
git add base/views.py base/urls.py templates/admin_user/loyalty/index.html templates/admin_user/sidebar.html base/test_fidelite.py
git commit -m "feat: dashboard fidélité (cartes + récompense donnée) + sidebar, gating Pro

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review (couverture spec)

- **Modèles (programme, carte, idempotence)** → Task 1. ✅
- **Service award/redeem/progress** → Task 2. ✅
- **Accrual au paiement (cashier)** → Task 3. ✅
- **Config Paramètres (Pro)** → Task 3. ✅
- **Affichage progression client (succès)** → Task 3. ✅
- **Dashboard liste + redeem + sidebar + gating** → Task 4. ✅

**À surveiller :** le libellé « encore N pour… » dans confirmation.html — calcul
`required - stamps` : si l'implémenteur ne peut pas faire l'arithmétique en
template proprement, exposer `remaining` dans le dict `progress` (ajouter
`"remaining": max(0, required - stamps)` au service et l'utiliser). Résolution
multi-tenant des tests de vue (aligner `_host()`).
